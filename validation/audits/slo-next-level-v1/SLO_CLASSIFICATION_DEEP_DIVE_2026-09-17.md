# SLO Classification Deep Dive — 2026-09-17

Conventions: `OBSERVED:` cited fact → `INFERRED:` conclusion (confidence) → `UNVERIFIED:`. Claim grades A–F per audit rubric.

## 0. Architecture (verified)
OBSERVED: `SmartSampleManager/CMakeLists.txt:1-2,82-134` (CMake 3.22, C++20, JUCE 8.0.2, umappp 3.3.2, dr_libs wav-0.14.5, hnswlib 0.8.0, ONNX Runtime via find_path/find_library + brew error `:165`, TagLib/libsodium/sqlite3/Accelerate); presets `ssm-dev/qualification/release-candidate/sanitize` (`CMakePresets.json:4-60`); plugin `juce_add_plugin ... FORMATS VST3 AU Standalone PRODUCT_NAME "SLO"` (`:476-493`); dual core libs `ssm_engine_core_prod` vs `ssm_engine_core_test (+SSM_TEST_BINARY=1)` (`:746-754`); ~20 direct + ~30 helper test executables (`TestSampleEngine…SafetyRegression`).
Flow OBSERVED: WAV/drag-drop → `SampleManagerEngine::prepareFile` (`SampleManagerEngine.cpp:4205`) → dr_wav decode (`:3348,3370`) → mono resample 32 kHz/160k = 5 s (`:4265,3813,5180`; `SampleManagerEngine.h:848`) → `analyzeAudioBuffer` DSP (`:3191`) → batched ONNX queue (`:4246`, `test_inference_batch_flush_main.cpp:6`) → ONNX `input {1,160000}→output {1,512}` (`:5188,5200`) → `AcousticClassifier::classify520/classify512` (`AcousticClassifier.h:210,351`) → per-class centroid OOD gate (`:338-343`) → `MlOverrideGate` filename/DSP override (`MlOverrideGate.h:32,72,90`) → `AbletonTaxonomy` loop/one-shot + `ClassificationPresentation` (`AbletonTaxonomy.cpp:75,119`; `ClassificationPresentation.h:77,124`) → SQLite WAL (`SampleManagerEngine.cpp:1197`) + HNSW/UMAP → JUCE UI → optional confirmed Sort (`reorganizeSamples` line 4779+, B-011 Copy/Move/Cancel + journal).
INFERRED (HIGH): plumbing is complete and coherent; no placeholder layers. Next-level work is model/taxonomy/eval, not scaffolding.

## 1. Encoder: PANNs CNN10 512-D (Grade B — verified by code, research-compared)
OBSERVED: `Models/panns_cnn10_embedding.onnx` (84 KB) + `.onnx.data` (24 MB); `SampleManagerEngine.h:140,195` versioned `embeddingModelVersion`/`classification_model_version`; `AcousticClassifier.h:13,23` 520-D = 512 PANNs + 8 DSP; `:66` NaN/Inf/zero/norm guard; `:112,185` fc1+ln1+gelu + 3×GatedResBlock + ArcFace cosine head; DSP norms e.g. `dsp[1]=min(centroid/16000,1)` (`SampleManagerEngine.cpp:294`), `dsp[4]=min(rms*5,1)` (`:297`); resample is engine linear-interp (`test_embedding_quality_main.cpp:6` REAL dr_wav→resampler→ONNX); `PluginProcessor.cpp:22` CoreML EP compile off ctor → `initAsync`; `test_sort_preview_and_undo_main.cpp:79` notes ONNX/CoreML version variance.
Controlled bake-off OBSERVED (`SLO_ENCODER_RESEARCH_DECISION_2026-09-15.md`): 21,793 rows, same DSP/ClassifierV4, 5-fold OOF seed 42: PANNs+DSP 73.86%/69.98% vs CLAP+DSP 82.20%/79.27%, Δ +8.34pp/+9.29pp, every class improved (Vocal Phrase +4.00 … Music Loop +19.19). CLAP cost: 744 MB dir / 776 MB weights vs 23 MB PANNs; row-stratified (pack-leakage inflates both); no C++ prod path, no OOD/blind/latency/license.
Production CLAP OBSERVED: zero hits in `Source/`; research-only in `tools/` (`clap_zeroshot_audit.py`, `export_clap_audio_onnx.py`, `compare_encoders.py`, `encoder_bakeoff*.py`).
INFERRED (HIGH): PANNs path is shippable-weight, correctly versioned; CLAP is a proven-accuracy / unproven-product bet.
Gap to pro-level: vendor-held-out bake-off; C++ CLAP/quantized path + cold-start/RSS/latency budget; license review. Until then PANNs frozen per decision doc.
UNVERIFIED: resampler quality vs SoX/polyphase on 44.1→32k transients; CoreML partition parity numbers.

## 2. Head + taxonomy (Grade C — partially present)
OBSERVED: 16-class frozen head (`AcousticClassifierWeights.h:1`, `Centroids.h:1` 16 centroids + per-class thresholds); `confidence = softmax top-1` (`AcousticClassifier.h:290`); `r.isOod = bestCos < perClassThreshold` (`:343`); `classify512` zero-DSP compat (`:351`). Subtype add-ons reuse raw-512D centroids without touching frozen head/OOD (`HiHatTypeClassifier.h:10`, `BassTimbreClassifier.h:10`; HiHat 48/52=92.3% note; Bass `CALIBRATION HALF ONLY of 7-vendor`).
Accuracy OBSERVED (irreconcilable without split labels — this IS the finding): benchmark 96.54%/96.45%/88.29% adversarial (`slo_classification_metrics_v1.json`, SUPERSEDED 2026-09-08) → B-006 cross-vendor 620-file 62.9% full / 30.2% audio-only → golden 58.8% full / 5.9% audio-only / 58.8% filename-only / 0% adversarial → v3 collection-held-out 57.01%/49.37% (25 classes) → PANNs OOF 73.86% (row-stratified, leaky). Class-by-class: benchmark F1 0.918–1.0 (Bass One-Shot 0.918 weakest) vs V4-H 76.4% known, Bass Loop F1 0, Synth Loop F1 0, Vocal Loop recall 7.5% (filename outranking ML), Vocal Phrase 86%.
INFERRED (HIGH): head overfits naming/vendor; loop family + vocal-loop are the taxonomy crisis; single headline accuracy is meaningless — must report audio-only / fused / adversarial splits always.
Gap: loop/one-shot repair, multi-label need assessment, cross-vendor per-class gates, separate audio-only vs fused surfacing (see backlog F-02).

## 3. OOD/UNKNOWN calibration (Grade C)
OBSERVED: shipping = softmax + entropy + centroid-cosine + V4-H per-class thresholds (`SLO_OOD_UNKNOWN_REPORT_V1.md`); V4-H single-vendor: false-known 41.6% (reject 58.4%, accept 96.8%), holdout 47.2%/52.8%/97.3% (~26–50/class, 125 OOD); cross-vendor B-007: false-known 72.0% (worse), false-unknown 17.7%; score-only AUROC ~0.697, FPR@95 0.78–0.81 (research comparator); safety net = visible UNKNOWN + conservative defaults + no auto-move (`ClassificationPresentation.h:77`; `BetaDecisionPolicy.h:156` confidence gate; `MlOverrideGate.h:90` OOD+DSP/UNKNOWN/empty → ml_ood/UNKNOWN/0.0; `MlOverrideGate.h:18` override floor ~0.40); input-safety unit-tested (`test_acoustic_classifier_input_safety_main.cpp:62` NaN/Inf/zero → OOD, no JUCE/ONNX dep); `workspace/slo_check_ood_calibration.py` (54 lines), `slo_score_scan.py` (105 lines) exist; `slo_main_scan_clean.json` 21 MB single-line + `slo_main_scan.json` 19 MB not loaded per cost discipline.
INFERRED (HIGH): gate logic correct, thresholds uncalibrated for open world. CLOSED in register means "measured + UI-mitigated", not fixed.
Gap: cross-vendor + adversarial-name calibration curves, per-class threshold re-fit (frozen pending Jack auth), coverage@95/90 reporting as ship gate (v4b coverage@95 only 13.4%).

## 4. Fusion: filename/folder/metadata (Grade B, double-edged)
OBSERVED: `AbletonTaxonomy.h:36,50` filename/folder loop tokens; `.cpp:75` both-agree → 0.85 (96.4% held-out); `:119` heuristic block upstream in prepareFile; `:324,394` token + tempo/key-suffix + tempo-marker evidence; `winningEvidence ∈ UNKNOWN/USER_OVERRIDE/EMBEDDED_METADATA/FILENAME/FOLDER/DSP` (`AbletonTaxonomy.h:33`); `MlOverrideGate.h:72` DSP/FOLDER/empty + confident non-OOD → override to ml_v3; `TestAcousticClassifierParity.cpp:120` FILENAME tier never ml_ood; research `fusion_classify.py:13` duration overrides disagreeing filename (6 s 'kick' → Kick Loop); `hierarchical_classify.py:1` V5 cascade; `waveform_fusion_current_benchmark.py`.
Effect OBSERVED: fusion lifts 30.2%→62.9% (B-006) but golden filename-only = full (58.8%) and adversarial = 0% — fusion IS the accuracy and the brittleness.
INFERRED (HIGH): keep fusion, but evidence-typed + ablated + adversarial-tested; never let FILENAME silently outrank acoustics on loop family (the Vocal Loop failure mode).
UNVERIFIED: TagLib BPM/KEY priority weights on real multi-vendor corpus.

## 5. Similarity search (Grade B — backend verified, UX gap)
OBSERVED: HNSW 0.8.0 + UMAP 3.3.2 pinned (`CMakeLists.txt:98-123`); `SampleManagerEngine.h:349,351,439` L2-512D + shared index + overfetch 5; 6/6 PASS (`TestFindSimilar, Weighted, NearDuplicates, EmbeddingQuality, TimbreRefinement, ReferenceSearch`); kicks-vs-noise proven through real chain; `findSimilarToReference` one-off ONNX + HNSW (`test_reference_search_main.cpp`); UMAP persisted (`umap_x/umap_y`), QuadTree map (`QuadTree.h`), aspect-scored retrieval (`AudioSimilarity.h`).
INFERRED (MEDIUM): backend ready; "doesn't prove creative usefulness or combined queries" per report — gap is Task-6 combined-query UX ("darker impacts", "shorter 808s" = similarity + Bright/Dark + duration + subtype, tested separately, never together).

## 6. Scan/index pipeline (Grade B; WAV-only is scope, not bug)
OBSERVED: central `prepareFile` (`:4205`), pendingFiles queue (`:2209,2256,2264`), 250 ms coalesced UI (`:2310`), hydration thread off UI/audio (`:434`, `SampleManagerEngine.h:1146`), bulk `hydratePersistedSamples` (`:1768`); SQLite WAL (`:1197`), rich evolving schema (`:1254-1381`: sample_cache + content_hash, taxonomy+confidence+evidence, 512-D BLOB, DSP sidecar, umap, versions, favorites/history/collections/overrides, cache_meta); 3-way versioning + bump-reanalyze-not-wipe + quarantine-on-corrupt; content-hash FNV-1a-64 of decoded mono (`SampleManagerEngine.h:174`); true duration preserved (`:188`), tempo gated on true duration (V4-F fix `:3878`); WAV-only admit (`addPathToQueue *.wav`, dr_wav `:3348-3370`); AIFF/FLAC/MP3 NOT supported (report verdict: solid for WAV, format = Jack scope decision); corrupt WAV safe (`TestMalformedAudio`); unsupported extensions silently invisible; duplicates via decoded-PCM hash (`findDuplicateGroups`); mtime re-analyze, prune-missing invalidates HNSW (`TestPruneMissing`); TagLib probe + coalesced async write queue (`test_format_aware_scan_main.cpp:40`, `SampleManagerEngine.h:946`); format-aware test admits WAV/AIFF/FLAC at harness level but prod scan does not.
Perf OBSERVED (split): 170-file cold 25,043 ms / cached 921 ms / RSS peak 2,439 MB / after 1,360 MB (HISTORICAL fixture) vs B-009 100/1k/10k + 30-iter soak clean + RSS plateau (needs single regenerated receipt).
Safety OBSERVED: `TestReadOnlySafetyQualification` PASS 4/4 SHA-256 identical; only mutations = own SQLite + explicit Sort (`reorganizeSamples` 4779+, copy-default per B-011); multi-instance SQLite tested; `production_cache_guard` aborts before `~/Library` in tests.
Gap: user-visible skip messaging; incremental rescan visibility; regenerated 100/1k/10k perf receipt on clean machine.

## 7. RT/UI thread safety (Grade B with limits)
OBSERVED: `PluginProcessor.cpp:60,108,120,139,148,159,199` — processBlock clears bus, pointer-handoff only, `pendingPlayback.exchange()` lock-free, `BufferingAudioReader(32768)+timeout 0` never blocks, message-thread reader prep; atomics `dawBpm/isDawPlaying` (`PluginProcessor.h:83`); no I/O/alloc beyond pointer swap on audio path (`:68`); engine/inference/sort off-thread (ThreadPool/locks/atomics/joins); all `ScopedLock dbLock/cacheDbLock/queueLock/inferenceQueueLock` non-RT (`SampleManagerEngine.h:798,974,1047`); ONNX init mutex init-only (`:2091`); TagLib/decode never on callback (`:4277`); `TestRtDeadlineStress` 2000 iters @512/44.1 kHz 0 misses 0 allocs (thread-local new/delete counter), rerun max 1.085→0.127 ms after atomics+lock-free+readahead. Limits per audit: one path/block-size, no multi-instance contention, B-004 Ableton matrix still OPEN.
INFERRED (HIGH): design is RT-correct; qualification is single-point, not matrix. Verdict PASS WITH LIMITATIONS, NOT QUALIFIED stands.

## 8. Data & eval harness (Grade C)
OBSERVED: `tools/classification_benchmark/` C++ harness (`scan|perf|soak|features|stereo|tempo|organize` + mach RSS), `run_benchmark.py` (golden precision/recall/F1/confusion), `run_real_corpus_{,v2}_benchmark.py` (B-006 leakage-controlled ablation), `train_gpu_classifier.py` + v2/v3/v4/v6 generations, `*_bakeoff*.py` (PANNs/CLAP/Perch/BEATS), `test_classification_benchmark_receipt_contract.py`; workspace `slo_main_chain.sh` (41 lines), `eval_90_precision_gpu.py`; 1619 fixtures + `parity_references.json` 572 KB; build logs 5 MB ×3 + `slo-test-list.txt` 41 entries; `Test*` inventory §3/appendix (47/47 executables claimed in final sweep 2026-09-15; `slo_test_results_v1.json` older snapshot shows dup-link FAIL era).
INFERRED (HIGH): harness is extensive but split-brained (leaky benchmark vs cross-vendor vs collection-held-out vs OOF) with no single vendor-held-out gate. Repro from clean checkout requires immutable Release rerun (B-002).
UNVERIFIED (could break silently): cache-migration paths on version bump under concurrent instances; UMAP stability across hydrations; TagLib write-queue backpressure on 10k+ batches.
