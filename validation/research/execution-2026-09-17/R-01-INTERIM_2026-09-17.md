# R-01 INTERIM — SLO strict-blind gate, engine side (2026-09-17)

## What ran (commands + observed)
- Gate corpus verified: `products/slo/_artifacts/slo_strict_blind_class_gate_v2/` — 72 wavs + `review_queue.csv` + `bundle_receipt.json` + `evaluator/candidate_mapping.json`.
- Engine staged predictions (from mapping, not a new inference run — engine already ran pre-blinding):
  - n=72 · classes: Kick 40 / Snare 26 / Clap 6 · confidence min 0.664 / mean 0.814 / max 0.909 · UNKNOWN staged: 0 · vendors: 8 (KSHMR Vol.3: 26, Organic Drum Kit: 13, Lo-Fi Memphis 2: 9, rest ≤6).
- Filename-proxy check (methodologically DIRTY — see below): 70/72 proxy-known, 70/70 agree (100%), mean conf of agrees 0.812.

## Honest interpretation
- The 100% proxy agreement does NOT measure acoustic accuracy. It shows staged candidates track source/filename metadata — consistent with the known filename/folder-evidence fusion in the pipeline, and exactly the leakage the blind protocol (filename hidden, `labels_created: false`) is designed to factor out.
- Threshold read: min staged confidence 0.664 with zero UNKNOWNs means the current gate never abstains on this set — the OOD/UNKNOWN calibration question (R-01 core) cannot be answered until human labels exist.
- Deliverable: `RESULTS_R-01-engine-side.csv` (72 rows: candidate, confidence, similarity, vendor, proxy label, proxy-agree flag, sha256).

## BLOCKED_ON_HUMAN — the actual gate
- `review_queue.csv`: 72 `human_label` cells EMPTY. Owner listens to the 72 anonymized wavs (~2–3h), fills best-frozen-class or UNKNOWN/OOD per the review prompt (audio only).
- The moment labels land: accuracy = agree/72, UNKNOWN rate, per-class confusion, confidence-vs-correct curves, and the ship/hold call (top-1 ≥70%, UNKNOWN ≤15%) compute in minutes. Repro: join labels to `RESULTS_R-01-engine-side.csv` on id/sha256.

## CLAP AB — scoped, not run
- Assets present locally: `clap_model/` 589M, `clap_model_music/` 744M, `clap_input_features.npy` 141M, `clap_onnx/` dir, `export_clap_audio_onnx.py`, `clap_zeroshot_audit.py`.
- Blockers in this env: no torch (system python3), audit script targets CUDA GPU (Apple Silicon here); viable path is CLAP-ONNX via onnxruntime 1.30.0 (present) on CPU — a separate multi-hour task. Recommendation: run CLAP-ONNX zeroshot on the same 72 AFTER human labels exist, so all three opinions (engine, CLAP, human) resolve at once.

## Decision: HOLD (pending human labels) — engine side complete, gate unblocked by ~3h of listening, not by more code.
