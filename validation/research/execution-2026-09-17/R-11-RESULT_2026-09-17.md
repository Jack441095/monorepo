# R-11 RESULT — licence clearance + cost model (2026-09-17)

Register: `R-11-LICENCE-REGISTER.csv` (19 rows). Licence inventory by source read (no prior-report trust).

## Ship-blockers (in order)
1. **P0 — JUCE commercial licence missing.** SLO + KENN vst3 build on JUCE 8.0.2 (FetchContent, all three CMakeLists), dual-licensed AGPLv3-or-commercial, and `THIRD_PARTY_NOTICES.txt:11-18` records the commercial purchase as OPEN with an explicit do-not-distribute. No closed-source sale of SLO or the KENN plugin until this is bought. (DiskSweep is pure C++/Swift — unaffected. Submit is Swift-only — unaffected.)
2. **P1 — PANNs weights distribution right unconfirmed.** Code MIT, AudioSet CC-BY-4.0, but the 23M `.onnx.data` blob has no licence file; `Models/README.md` says no restrictive Zenodo licence was found — that is not a clearance. One legal-confirm task before the weights ride in a `.dmg`.
3. **P1 — testing-assets quarantine.** 57G of commercial packs (KSHMR, Shinoda, Black Octopus, …), zero licence files. Internal eval use only; nothing from this tree may enter a bundle, repo, or model-training set without per-pack rights. (R-01's blind gate uses KSHMR clips as *test inputs* — fine; shipping them would not be.)
4. **P2 — version pins + SBOM.** TagLib/ONNX/libsodium are brew-unpinned (works-on-my-machine risk + unattributable version); Python backend needs a generated SBOM pre-launch (no GPL names spotted, unverified); Minilm/Kokoro need a bundle decision; OpenSSL/libjpeg-turbo need bundled texts (links only today); AbletonOSC MIT needs a bundled-attribution check at pack time; Sansation txt must ride in every dmg.

## Cost per seat (no cliffs found — local-first pays off)
- Payments: 5% + 50¢/txn (Paddle today, Polar same) — only marginal cost that scales with revenue.
- Inference: £0 marginal — Ollama runs on the user's Mac (KENN, DiskSweep); PANNs embedding on-device (SLO); no server GPU, no per-seat API spend. Cloud fallback (OpenAI) is now opt-in only (D-02), so no surprise bills.
- Backend hosting (Railway/Render), email, download storage: flat-ish infra, sub-£100/mo at beta scale (exact: confirm current Railway/Render invoices — the one number I could not source locally).
- No 10x-scale cliff identified: costs grow with transactions (%), not with usage (inference is free at the edge).

## Decision: CONDITIONAL-GO for paid launch on licensing — Submit and DiskSweep are clear; SLO + KENN plugin wait on the JUCE purchase (+ PANNs confirm for SLO).
