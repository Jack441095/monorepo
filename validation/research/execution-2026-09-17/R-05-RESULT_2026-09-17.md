# R-05 RESULT — DiskSweep safety mechanics (2026-09-17)

Harness: `R-05-harness.py` (rules-only, isolated HOME — nothing touched the real disk).
Corpus detail: `RESULTS_R-05-corpus.csv` (22 rows).

## Numbers
- Spec-conformance corpus: **22/22** (BLOCKED 7/7, REVIEW 8/8, SAFE 7/7) in 0.1ms total (~0.00ms/item rules-only).
- Adversarial guard fuzz: **5000/5000** `classify()` final-guard consistent with `is_blocked()` across prefix/`..`/`.`/trailing-slash/case mutants.
- Trash end-to-end (isolated HOME): SAFE moved to `~/.Trash/Disksweep/<stamp>/`, REVIEW kept without flag, BLOCKED skipped, `undo.json` lists only the SAFE move, `cmd_undo` restores bytes, zero writes outside Trash.
- Existing suite re-confirmed: 31 passed.

## Observations (not failures)
- Case: `/SYSTEM/x` → REVIEW (blocklist is case-sensitive; scanner emits real paths so exploitability is LOW, but a hardening one-liner exists — normalize case in `_norm`). Graded P3.
- `..` escape (`/usr/../tmp/evil`) normalizes OUT of the blocklist to REVIEW — correct behavior (path no longer under /usr), verified, not a bypass.
- LLM enrichment path (Ollama qwen models) NOT exercised — no Ollama here. Rules-only is the deterministic ship path (`--no-llm` exists); enrichment only appends reason text and can never clear BLOCKED (code-guaranteed + final guard).

## What this is NOT
Real-world accuracy (SAFE-vs-REVIEW judgment on real disks) still needs a human-labelled corpus — same ears-needed story as R-01, smaller (spot-check ~50 real items). Privileged-helper threat review + DMG build remain open (flagged in baseline).

## Decision: CONDITIONAL-GO on safety mechanics for beta.
BLOCKED precision on spec: 7/7 + 5000/5000 guard-consistent. No Trash-outside writes. Remaining beta gates are packaging (DMG), helper review, and a 50-item real-disk spot check — none of which move these numbers.
