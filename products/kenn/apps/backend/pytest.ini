[pytest]
# AbletonOSC is vendored as a Live runtime dependency. Its upstream tests
# assume the upstream checkout is on sys.path and are not KENN test targets;
# the KENN installer has its own runtime-copy checks.
norecursedirs = integrations/ableton-osc UX
pythonpath = src ../../tooling
