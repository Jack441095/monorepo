[project]
name = "kenn"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["nite-core", "audio-analysis"]

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["kenn*"]
