# KENN gap and defect register

Statuses are based on fresh source/runtime evidence, not route names or historical receipts.

| ID | Pri | Status | Evidence and impact | Required completion gate |
|---|---:|---|---|---|
| KENN-001 | P0 | Fixed 2026-09-21 | The canonical source is tracked, internal compatibility links resolve, private/generated assets are excluded, and detached-checkout Python, frontend, native-kernel and JUCE verification passed. | Keep the new core/native workflows green on the published review branch. |
| KENN-002 | P0 | Open | No real-Live mutation/readback/undo run; real probe returned AbletonOSC offline. | Disposable `.als`, real proposal/confirm/mutate/readback/undo receipt for each risk class. |
| KENN-003 | P0 | Fixed | Alternate FastAPI allowed `*` origins with credentials while exposing command routes. | Local allowlist regression passes; still keep service local until auth/rate limits exist. |
| KENN-004 | P1 | Fixed | Scoped chat imported a removed `Audio_Too` checkout and its requirements included a nonexistent file. | 39/39 tests pass from documented command. |
| KENN-005 | P1 | Open; diagnostics fixed | Active index lacks `embeddings.npy`; runtime uses BM25 only and now exposes `kenn.retrieval_status.v1` through health surfaces without claiming semantic activation. | Reproducible index build, digest/version receipt, semantic recall and latency gate. |
| KENN-006 | P1 | Open | Official Live manual has zero indexed chunks. | Authorized manual ingestion and grounding evaluator passes with provenance. |
| KENN-007 | P1 | Open | Default server and alternate FastAPI have materially different route sets and policy behavior. | Choose one public runtime or contract-test full parity; document deprecation. |
| KENN-008 | P1 | Open | In-memory proposals/idempotency plus process-bound confirmations fail closed after restart but cannot reconcile uncertain mutations automatically. | Durable action state machine keyed by session/correlation/idempotency with startup reconciliation. |
| KENN-009 | P1 | Open | AudioGen producer is external/optional; actual generation, audition, Live insertion and undo are unqualified. | Versioned producer handshake and real end-to-end qualification. |
| KENN-010 | P1 | Open | SLO source is absent from main checkout and no verified production manifest is installed. | Signed/versioned manifest, model/taxonomy hashes, OOD calibration and large-library handoff test. |
| KENN-011 | P1 | Fixed locally 2026-09-21 | `/usr/local/bin/node` is x86_64, but the workspace-provided arm64 Node completed fresh `npm ci`, a zero-vulnerability audit, 5/5 Vitest tests, and the production build. | Browser E2E and remote workflow execution remain release gates. |
| KENN-012 | P2 | Open | Deterministic intent holdout is self-bounded and does not establish open-ended musical reasoning. | Frozen external holdout, human rubric, blind adjudication and failure registry. |
| KENN-013 | P2 | Open | Arrangement evaluator has four synthetic cases only. | Real project graph corpus covering sections, groups, automation and contradictory evidence. |
| KENN-014 | P2 | Open | Multi-user/project preference isolation lacks adversarial qualification. | Tenant/project namespace tests, retention/deletion tests and prompt-injection corpus. |
| KENN-015 | P2 | Open | Native DSP is opt-in despite decisive fresh audio-analysis results; cross-platform wheel/sanitizer gates remain incomplete. | CI wheels, ASan/UBSan, fallback diagnostics and representative corpus parity. |
| KENN-016 | P2 | Rejected now | Native Mix Review loudness only improved p50 1.13x on the complete request. | Keep opt-in; reconsider only after profiling a representative corpus. |
| KENN-017 | P2 | Open | Plugin stress result is not TSan and benchmark covers meter analysis, not full callback/host behavior. | TSan where supported, pluginval/Live host runs, deadline/allocation instrumentation. |
| KENN-018 | P2 | Open | Multiple divergent backend copies create stale-code and import-root risk. | Archive/delete only after owner review; contract-diff and import-path CI first. |
| KENN-019 | P3 | Open | Historical docs include claims such as “Verified” that exceed current real-Live evidence. | Generate capability docs from qualification receipts and label historical material. |
| KENN-020 | P0 | Fixed 2026-09-21 | `audiogen_artifacts._canonical_midi_notes` recorded range errors but still appended the invalid note, leaking out-of-range pitch/velocity and non-finite timing into artifacts. Masked only by downstream `safe_artifact_metadata` guards. | `continue` after each error branch; `test_audit_p0_regressions.py::test_canonical_midi_notes_drops_invalid_notes_instead_of_leaking_them` passes; final full suite 1,275 passed. |
| KENN-021 | P0 | Fixed 2026-09-21 | `idempotency_bounds.prune_if_needed` called `set.clear()`, wiping the entire 10k replay-protection window; any recent action id could replay a write after the bound was hit. | Evict-half + prune-before-add in the affected services; fresh key guaranteed retained; `test_idempotency_bounds.py` (5 tests) passes. Durable cross-restart replay protection remains KENN-008. |
| KENN-022 | P0 | Fixed 2026-09-21 | `LiveExecutor.undo_batch_action` returned `ok:True/status:undone` even when only some restore steps succeeded; callers could not distinguish partial undo. | `ok` now requires all steps; partial returns `ok:False/status:partially_undone` with counts; regression tests pass. |
| KENN-023 | P1 | Fixed 2026-09-21 | `MidiClipActionService.propose_create` accepted notes with `start+duration` beyond the clip length (the audiogen path enforced the bound; this direct path did not). Undefined Live clip-overflow behavior. | Reject with explicit error naming the offending note and clip length; boundary note ending exactly at length accepted; regression tests pass. |
| KENN-024 | P1 | Open | `live_intent.parse_request` blanket-`refuse`s any request containing delete/remove/overwrite/replace/destroy/erase, so NL track deletion, device removal, and clip replacement are unreachable even though `midi_clip_service` supports confirmed removal proposals. Destructive NL ops should be confirmation-gated, not refused. | Route destructive NL intents into the proposal+confirmation+readback path instead of `mode:refuse`; qualify each risk class against KENN-002. |
| KENN-025 | P1 | Fixed 2026-09-21 | `generative_midi.py` allowed invalid/unbounded inputs, and the reachable frontend groove/bassline handlers called obsolete function signatures. | Central note/range/size/finite validation, style compatibility, HTTP 400 behavior and actual HTTP route regressions pass. |
| KENN-026 | P1 | Fixed offline; Live pending | Batch replies previously fell back to arrival order and topology/mutation snapshots could use the 1.5 s cache. | Ambiguous replies are dropped unless address or echoed arguments identify exactly one request; topology and action snapshots force refresh. Out-of-order, ambiguity and freshness regressions pass; real Live remains KENN-002. |
| KENN-027 | P1 | Fixed 2026-09-21 | Nested OSC bundles had no packet/depth/element bound; consumed confirmation tokens had no hard live-set ceiling. Proposal/receipt stores already had cleanup caps but the diagnostics misstated the proposal limit. | 65,507-byte packet, depth-8 and 4,096-element caps; confirmation registry fails closed at 10,000 live tokens; diagnostics corrected; adversarial regressions pass. |
| KENN-028 | P1 | Fixed 2026-09-21 | `benchmark_audio_analysis.main()` returned exit code 0 even when its computed qualification failed, allowing CI to accept a failed performance gate. | Exit 0 only for a qualified result; benchmark contract regression passes and receipts retain the verdict. |
| KENN-029 | P1 | Fixed 2026-09-21 | `/api/midi/groove` passed four keyword arguments not accepted by `apply_audiogen_groove`; `/api/midi/bassline` passed obsolete `root_pitch`/`scale` arguments. The shipped frontend path could raise before returning JSON. | Frontend payloads now execute through the real HTTP server, produce bounded MIDI, and return 400 for invalid input. |
| KENN-030 | P2 | Fixed 2026-09-21 | Normal HTTP client disconnects raised uncaught `BrokenPipeError` from JSON/static/stream body writes, polluting server logs and worker traces. | One bounded body writer handles disconnect/reset/abort, terminates streams, and passes server-smoke plus focused regression tests. |

The remaining external runtime blocker is KENN-002 (real Ableton/AbletonOSC). KENN-001 is locally closed by clean-checkout evidence; the published workflow result remains the independent repository gate.
