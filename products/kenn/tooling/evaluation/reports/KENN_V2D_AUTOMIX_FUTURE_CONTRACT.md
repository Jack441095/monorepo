# Future Automix Contract

Automix remains unauthorised. A future eligible path is: measured evidence →
qualified recommendation → scoped proposal → preview → explicit approval →
bounded reversible execution → receipt → undo → post-action validation. No
V2-D detector result can invoke a DAW write or an Automix parameter change.
