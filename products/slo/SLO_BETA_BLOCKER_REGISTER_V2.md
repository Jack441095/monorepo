# SLO Beta Blocker Register V2

**Updated:** 2026-09-14
**Supersedes:** `SLO_BETA_BLOCKER_REGISTER_V1.md` (kept for history; this is the current source of truth). Full evidence trail for every closed item is in `NITE_DSP_SLO_MASTER_PLAN_V1.md`, `_V2.md`, `NITE_DSP_SLO_FINE_SUBCATEGORIZATION_V1.md`, and this repo's `docs/classification/` reports — this register states outcomes, those files carry the receipts.

| ID | Severity | Blocker | Status | Evidence | Owner action needed |
|---|---|---|---|---|---|
| B-001 | P0 | Developer ID signing, notarization, installer/update flow absent | **OPEN — blocked on Jack** | No signing identity exists. | Apple Developer ID enrollment (long lead time — start regardless of beta timing) |
| B-002 | P0 | Clean-machine validation incomplete | **OPEN — blocked on Jack** | Needs a physical/VM Mac this session has no access to. | Run install/launch/uninstall on a clean machine |
| B-003 | P0 | Production licensing endpoint/credentials absent | **OPEN — blocked on Jack** | Default is dev localhost HTTP. | Real production credentials, HTTPS endpoint |
| B-012 | P0 | 13,044 duplicate JUCE symbols broke the clean build | **CLOSED** | `ssm_qual_full` verified clean twice (post-fix, and again on the final fully-merged state), zero duplicate-symbol occurrences. `NITE_DSP_SLO_MASTER_PLAN_V1.md` Phase 1. | none |
| B-004 | P1 | Ableton Live VST3 host matrix not qualified | **OPEN — blocked on Jack** | Needs a live GUI DAW session, not scriptable from here. | Run the scan/load/render/state/drag/reopen matrix in real Ableton |
| B-005 | P1 | Blind AL-002 review incomplete | **OPEN — blocked on Jack, not an engineering task** | `AL-002` is an explicitly protected/sealed evaluation set ("no protected labels or predictions are exposed... no completion receipt was available to authorize a pass claim"). Fabricating a substitute would defeat the point of a protected holdout. | Jack must authorize/perform this directly, or define what a session-executable version would even look like |
| B-006 | P1 | Cross-vendor and leakage-controlled accuracy not established | **CLOSED (reporting)** | Real, honest 62.9% full-evidence / 30.2% audio-only accuracy on 620 real cross-vendor files, replacing the synthetic-set 96.5%/5.9% figures. `NITE_DSP_SLO_MASTER_PLAN_V1.md` Phase 2. No production classifier code changed — this closed the *honesty* gap, not an accuracy gap. | none for the reporting requirement; accuracy improvement is a separate, larger effort (see Taxonomy Requirements doc) |
| B-007 | P1 | OOD false-known remains high in thin historical evidence | **CLOSED (measured)** | First cross-vendor false-known measurement: 72.0% (worse than the 41.6-47.2% single-vendor baseline). False-unknown cross-check: 17.7%. Also fixed: UI showed identical text for "never scanned" and "genuinely Unknown." `NITE_DSP_SLO_MASTER_PLAN_V2.md` Phase 6. | Real fix needs recalibrating the per-class OOD threshold — **spec-frozen, needs Jack's explicit authorization to reopen** |
| B-008 | P1 | Vocal Loop failure in V4-H evidence | **CLOSED (verified + partially improved)** | Existing fix didn't generalize (0/22 cross-vendor). Vocabulary gap fixed (1/22), then the tempo-only/bracketed Loaded Samples slice was remediated to 2/2 in a measured 38-file scan; the acoustic head still disagrees and remains diagnostic-only. Broad cross-vendor accuracy is still unproven. See `docs/classification/VOCAL_LOOP_CROSSVENDOR_V1_REPORT.md` and receipt `tools/classification_benchmark/receipts/vocal_loop_tempo_marker_eval_20260914.json`. | Expand owner-reviewed cross-vendor evaluation before any accuracy or auto-action claim |
| B-009 | P1 | Peak RSS and long-soak/large-scale gates incomplete | **CLOSED** | Real 100/1k/10k-file scale receipts + 30-iteration soak test, all real audio. Soak is clean (RSS plateaus, no leak). `NITE_DSP_SLO_MASTER_PLAN_V1.md` Phase 3. | none |
| B-010 | P2 | RT timing/allocation instrumentation incomplete | **CLOSED** | Real instrumented test: 0/2000 deadline misses, 0 allocations across all tracked `processBlock()` calls, reproduced on a second run. `NITE_DSP_SLO_MASTER_PLAN_V2.md` Phase 8. | none for this evidence; Ableton validation (B-004) is still the gap for full RT sign-off |
| B-011 | P2 | Sort Library is move-based and rollback evidence is incomplete | **CLOSED** | Preview panel now shows the complete proposed operation and safety exclusions, exposes explicit Copy/Move/Cancel choices, runs the operation asynchronously with cancellation, and records a durable source→destination journal. `TestSortPreviewAndUndo` passes preview-no-mutation plus Move/Copy→Undo recovery; `TestSortLibraryAsync` passes cancellation, re-entrancy, and copy-preservation checks. | none |
| B-013 | P1 (new) | Scan/classify path's read-only-ness was never proven with real evidence, only claimed | **CLOSED** | `TestReadOnlySafetyQualification`: real SHA-256 checksums, before/after, on a disposable fixture library, exercising the actual production scan path. See `SLO_READ_ONLY_SAFETY_QUALIFICATION_V1.md`. | none |
| B-014 | P2 (new) | No producer-facing fine-grained subcategorization (808 vs Reese, kick length, etc.) | **IN PROGRESS** | First real, evidence-gated tag shipped (bass timbre, 92.9% leakage-free holdout accuracy). The verified research packet now has 795/795 identity-matched rows, but the metadata gate still blocks five frozen classes and thin/mixed source-family support. One-row/one-label matching confirms jointly feasible family coverage in both the original 487-row queue and a new 387-row supplement built from existing by-ear CSVs; five contradictory content hashes were blocked. The queues consolidate to 624 unique hashes, with two exclusion-aware 120-row master batches and a full 624-row option-filtered CLAP advisory receipt. A localhost-only reviewer, coverage planner, and duplicate-safe merger are available. No decisions are imported. See `SLO_PRODUCER_TAXONOMY_REQUIREMENTS_V1.md` and `SLO_REMEDIATION_PLAN_V1.md`. | Use the master batches and coverage plan to complete owner labels with explicit reviewer/note/status, then import, merge, rerun the preflight and sealed holdout before promoting any new subtype |

## Summary counts

- **Closed with real evidence**: B-006, B-007, B-009, B-010, B-011, B-012, B-013 (7)
- **Closed/verified but with an honestly-stated residual gap**: B-008 (1)
- **Blocked on Jack, not an engineering task**: B-001, B-002, B-003, B-004, B-005 (5)
- **In progress, real methodology proven, breadth remaining**: B-014 (1)

Nothing is marked closed without a specific evidence citation above — if a row doesn't point at a real report/test, it isn't actually closed.
